import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1A914B",        
      light: "#4CAF7D",
      dark: "#0f6b36",
      contrastText: "#ffffff",
    },
  },
});

export default theme;